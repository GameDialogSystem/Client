{{to-elsewhere named="tabs" send=(hash tabs=tabs) }}
{{to-elsewhere named="tabs-content" send=(hash tabs=tabs) }}


{{!--
{{to-elsewhere named="tab-content-line"
               send=(hash body=(component "paper-button") onOutsideClick=(action "mauz"))}}
--}}

{{!--
{{#ember-wormhole to="tab-content-line"}}
  {{#paper-button iconButton=true}}
    {{paper-icon "translate"}}

    {{#paper-tooltip position="bottom"}}
      Translations
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "grade"}}

    {{#paper-tooltip position="bottom"}}
      Set favorite
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "fingerprint"}}

    {{#paper-tooltip position="bottom"}}
      Make Unique
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "label"}}

    {{#paper-tooltip position="bottom"}}
      Add Labels
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "record_voice_over"}}

    {{#paper-tooltip position="bottom"}}
      Voice Recordings
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "format_bold"}}

    {{#paper-tooltip position="bottom"}}
      Format Bold
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "format_italic"}}

    {{#paper-tooltip position="bottom"}}
      Format Italic
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "format_underlined"}}

    {{#paper-tooltip position="bottom"}}
      Format Underline
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "format_textdirection_l_to_r"}}

    {{#paper-tooltip position="bottom"}}
      Text direction left to right
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "format_textdirection_r_to_l"}}

    {{#paper-tooltip position="bottom"}}
      Text direction right to left
    {{/paper-tooltip}}
  {{/paper-button}}

  {{#paper-button iconButton=true}}
    {{paper-icon "mood"}}

    {{#paper-tooltip position="bottom"}}
      Set Mood
    {{/paper-tooltip}}
  {{/paper-button}}

{{/ember-wormhole}}
--}}
{{flow-container
  blocks=model.lines
  deleteBlock=(action 'deleteBlock')
  onElementEdit=(action 'editDialogLine')
  connectToNewBlock=(action 'createNewDialogLine')}}
