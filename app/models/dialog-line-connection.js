import FlowConnection from 'flow-logic/models/connection';


/**
 * This empty class is a workaround for the server that returns
 * relations to dialog line connections. May be obsolete in future releases
 */
export default FlowConnection.extend({

});
